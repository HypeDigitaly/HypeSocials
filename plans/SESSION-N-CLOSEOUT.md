# SESSION N — CLOSEOUT (output language + final paid run + docs — v2.7.0, D63)

**Date:** 2026-08-21 · **Plan:** `plans/xmasterplan-render-quality-and-language.md` §5, §7 "SESSION N" (Waves 0N, 9a–9g, 9i — all executed; there is no 9h in the plan) plus SESSION M's "must do first" items 2–7. **Design contract:** `plans/SESSION-N-DESIGN.md` (the one interface every executor built against — read it before the code).
**Branch:** `session-k-colour-type-spine` continued (parent: M's `2f76c4b`). J → K → L → M → N are unmerged to `main` and merge together.
**Suite:** 1762 → **1910 passed, 0 failed** (+148: 22 copywrite translate/FR-313, 37 slide_intel, 29 outputs/estimator/console, 20 config/cli/menu/preflight/plan/filter, 7 Virlo channel, 2 parity/prompts-engine, 31 from the review-fix round).
**Growth:** production 37,128 → **39,614 (+2,486)** · tests 36,801 → **39,861 (+3,060)**.
**Live spend this session:** `--preview-sources` $0 (`20260821_030311_osj7`) · `--preview-analysis --carousels 9` **$0.69** (`20260821_030408_x0ug`) · the final 9-carousel paid run `20260821_030722_4344` — see "Final run" below.

## Wave status

| Wave | What | Outcome |
|---|---|---|
| 0N | PRDs v2.7.0/D63: D63 paragraph + table tick + registry + TL;DR + amendment-log entry (00-overview); FR-343 new, FR-294/FR-100-101/FR-313/FR-331 amended (10-pipeline); FR-293/FR-306 amended (20-integrations); FR-345 new, §2 key, CLI §5 row, FR-300/NFR-16/FR-333/FR-341 amended (30-config); FR-346 new, FR-73 amended (40-outputs); FR-344 new, FR-181 + §2 roster (50-promptcraft); PRD.html header + D63 card | ✅ — needed ONE correction round (no D63 paragraph, no FR-344/FR-345 blocks, three FRs not amended at their location, three factual slips in FR-343) plus conductor leftovers (CLI row, FR-300/NFR-16 notes, two wording slips). Memory note written so the next session budgets the round. |
| 1 — 9a | `virlo_mcp/server.py` both normalisers forward `language_detected` + `is_multilingual`; `sources/virlo.py` maps them → `SourcePost.language` (normalised by `topic_filter.language_code`) / `.multilingual`; `language_code`/`target_languages` public | ✅ 7 tests; no import cycle (`topic_filter`'s closure never reaches `sources`) |
| 1 — B | `sources/slide_intel.py`: FR-313 bare numerals (`_BARE_TOKEN`, `_bare_candidates`, rule 2 ONLY, `counter_line(text, *, position=0)`) + deck-level `language` (`_SCHEMA` strict `required`, `SlideIntel.language`, `_language()` via `language_code`) | ✅ 37 tests; the seven j867 panels now detect `positional`, pad 2 |
| 1 — prompts | `prompts/copy_translate_system.md` (new, 9,815 chars, eight placeholders), `slide_intel_question.md` +5 lines (deck `language`), `prompts/README.md` (eleven globals / sixteen shipped, `{{translate_panels}}` in four places), `styles.yaml` four `match_profile`s re-worded (§12 of the design) | ✅ measure_one_style unchanged for all four (profile prose is not `owned`), `registry_contract_check` TOTAL 0, `measure_prompt_fit` 0 of 26 outside |
| Conductor (between waves) | twins spliced with the NEW `plans/tools/splice_builtin_twin.py` (CRLF-preserving, byte-asserted); `_ALLOWLIST` row; `GLOBAL_TEMPLATES` + `PLACEHOLDERS` rows; `CopyTranslated`, two `DegradationTag`s, `AssetRecord.copy_language`/`.source_language`; `RunConfig.copy_language_mode`; `copywrite` `LANGUAGE_*`, `CopyProvenance.copy_language`/`.source_language`, `write_copy(copy_language_mode=, post_languages=)`; `SHIPPED_COUNT` 16 + parity roster; `test_prompts_engine` allowlist pins | ✅ Wave 1 barrier 1806 |
| 2 — A | `copywrite.py` translate path: `_source_language`, `_translate_wanted`, `_call_translate`/`_translate_schema`/`_translate_block`, `_translate_field` (no budget), `_translated_deck`, `_Translation`/`_translate_and_fit` (translate FIRST, then `_rows_over_budget` on the translated texts, then `_call_compress` on a derived offer), `_translated`, `_sibling_list` fifth branch, `translated: False` on every other walk, `_strip_counter_lines(position=)` at admission, module docstring point 7 | ✅ 22 tests; branches 1–4 and their pinned tests untouched |
| 2 — C | `config.py` field comment, `cli.py --copy-language`, `menu.py` confirm line + `_target_language`, `wizard_help.md`, `preflight._check_language_mode` + hint clause, `plan.off_language_post` (fourth `fresh_source_post` test + `_carousel_supply`), `topic_filter._apply` mode gate + docstrings + alias table, four configs (`target` pinned; cs comments rewritten ×3) | ✅ 20 tests; no `plan ↔ topic_filter` cycle |
| 2 — D | `budget.py` `translate_call` line + `_translating` + `estimate(post_languages=)` + widened retry/split allowances; `runner.py` `_write` args, COPY line shapes, loud `copy_not_translated`, `language` launch line, `translated` provenance row, `_post_languages`; `previews.py`; `outputs/gallery.py` `_any_translated` + chip + card line; `generate/__init__._record` | ✅ 29 tests |
| Barriers | Wave 1 1806 → Wave 2 1879 → `--preview-sources` $0 → `--preview-analysis` $0.69 → estimator lines at $0 → paid run | ✅ numbers below |

## What shipped

- **FR-343 translate pipeline.** Ladder `SourcePost.language` → `SlideIntel.language` (`write_copy(post_languages=)`) → the FR-294 screen's `Verdict.language` for the post's topic (`write_copy(topic_languages=)`, added by the review-fix round) → `""`. `_translate_wanted` = `target` ∧ `_panel_mapped` ∧ known language ≠ `entry.language`; unknown → verbatim + ONE `translate_language_unknown`. `_write_group` partitions `translating` / `compressing` / `selecting` (disjoint, exhaustive); one `_translate_and_fit` per translating entry under the same `gather`; the FR-99 `missing` split excludes translating entries. `_translate_field` has NO budget parameter (gates: blocklist, social-mark blank, `PANEL_SANITY_CHARS` → `translate_over_sanity`); `build_context(carousel_copy_mode="verbatim")` so the prompt never states `(at most`. Already-target backstop (source bytes ship, `translate_already_target`), length-drift audit (`_DRIFT_FLOOR` 0.5 / `_DRIFT_CEILING` 2.0 → `translate_length_drift` tag, ships), empty → wordless (`translate_no_text`), dropped-position answers discarded (`translate_invented_text`), `translate_list_truncated`. **Translate-before-auto:** `_rows_over_budget(deck.texts, budget)` on the TRANSLATED strings, then `_call_compress` on `dataclasses.replace(offer, panels=translated, stripped_panels=∅)` with `only=`; `_translated` runs `_auto_deck` on that offer, copies `translated` per row, empties every `ref_label`, `refs = {}`, ORs `stripped`; `copy_mode` = `compress` / `auto` (any row compressed) / `verbatim`. Caption/hashtags/headline always from the TRANSLATE payload (headline keeps its budget via `_compress_field`; caption via `_compressed_caption`). Whole-call failure → `_mapped_fallback` + `copy_degraded` + `copy_not_translated`; a failed follow-up compress → translated deck ships uncompressed + `translate_compress_failed` + `copy_degraded`. `quoted=()` → `_verify` half 1 self-skips, blocklist half runs.
- **FR-344 `copy_translate_system.md`.** Eleventh global role template / sixteenth shipped role (`SHIPPED_COUNT` 16); `{{translate_panels}}` allowlisted to it alone (`models.PLACEHOLDERS` + `prompts_engine._ALLOWLIST`); the compress template's fourteen style bans verbatim (item 14 "compression" → "translation"), identical fencing and hard bans; rules: translate never shorten (may be LONGER), unprinted → `""`, already-target → byte-identical + `source_language`, caption translated + humanised. 9,815 chars (the plan's 6,500 target is unreachable with the bans verbatim — accepted; one call per deck, ≈ 2.5K prompt tokens). `slide_intel_question.md` asks for ONE deck-level `language`.
- **FR-345 the mode.** `run.copy_language_mode: source | target` (engine default `source`; `hypedigitaly`/`-fresh`/`-cs` pin `target` — cs flips symmetrically: its English monitor's bound decks are now translated to Czech, three comment sites rewritten); `--copy-language`; confirm-screen `copy language: target - bound decks translated to en` (57 chars; `en/cs` when platforms disagree); `--yes` launch summary `  language    copy: target · bound decks translated to en`; `preflight._check_language_mode` warns under `target` about images/reels/override creatives; `plan.off_language_post` (the `source`-mode bind screen — fourth `fresh_source_post` test, `_carousel_supply` agreeing, ONE log line per dropped post from `_carousel_supply`, the German-deck fix); `topic_filter._apply`'s LANG skip fires only under `source` (docstrings no longer claim "no translation path"); `_LANGUAGE_ALIASES` widened (German/Deutsch → `de` …).
- **FR-346 receipts.** `CopyProvenance`/`AssetRecord`/`meta.yaml` `copy_language` (LANGUAGE axis; `target` only when a translation shipped) and `source_language` (recorded on every bound deck where known, both modes), per-row `panel_map[*].translated` on all four walks; gallery `_any_translated`, card line `Copy: translated from de to the platform's language — …`, per-row chip `translated from de` (beside `compressed from N chars` on an auto row); previews header `N deck(s) translated` + row kind `translated`; runner COPY line (`, N translated`; the both-fire shape drops the `N call(s) ->` head to stay inside `_stage`'s 53 columns), loud two-line `copy_not_translated` block (NOT in the exit-1 set), `translated P1 @handle views id de->en -> panel_map` provenance row; estimator `translate_call` line per deck (`_TRANSLATE_PROMPT_TOKENS` 3,200 · 400/panel in · 500/panel + 300 out; worst case every non-override carousel until the runner's post-ASSIGN re-price passes `_post_languages(kept)`), `copy_retry_allowance` + `copy_split_allowance` widened.
- **9a Virlo channel.** `language_detected` (68/100 videos, 82/100 slideshows in the fixtures) and `is_multilingual` forwarded by both wrapper normalisers; `SourcePost.language` is the ONE adapter-normalised field (a code about the strings, not one of them — D42 intact).
- **FR-313 bare numerals (M's defect 3).** `_bare_candidates` over `^\s*(\d{1,2})\s*$` lines take part in rule 2 ONLY (never rules 1/3/4 — a stray content numeral must not manufacture a constant offset); `counter_line(text, position=ordinal)` at admission drops the line into `chrome_counter_stripped`, `source_text_original` keeps it. The j867 `_03` deck's seven `01…07` lines: detected `positional`, pad 2, all seven dropped.
- **FR-341 narrowing (M's item 4).** `big-number-editorial` claims only panels that OPEN on their own ordinal; `photo-poster-statement` (unnumbered one-statement panels), `neon-glass-dark` (one tool per panel, review length), `aurora-white-deck` (corporate short rows) claim what they are handed. Preview result below: 0 of 9 on `big-number-editorial`, 7 distinct styles, no concentration line.
- **Tooling.** `plans/tools/splice_builtin_twin.py` — copies a `prompts/*.md` into `prompts_engine._BUILT_INS` (replace in place or insert after `--after`), preserves the working copy's CRLF under `core.autocrlf=true`, refuses templates with `"""`/backslashes, re-imports and byte-asserts. Verified as a clean no-op on `copy_compress_system.md` before first use.

## Barrier numbers

- **`--preview-sources`** (exit 0, $0, `20260821_030311_osj7`): launch summary prints `language    copy: target · bound decks translated to en` under the `carousels` line; `registry v1 · 26 styles · sha 067a78b6 · 17 usable here`; 9 eligible.
- **`--preview-analysis --carousels 9`** (exit 0, **$0.69**, `20260821_030408_x0ug`): matcher **9 of 9 (3 high, 6 medium)**, **7 distinct styles** — `neon-glass-dark` ×2, `circuit-atlas-dark` ×2, `platform-showcase-card`, `contrast-verdict-deck`, `build-log-mono`, `photo-poster-statement`, `icon-ledger-carousel`; `big-number-editorial` 0 (was 5 of 9 on M's preview); **no FR-355 concentration line**. Copy: 7 decks auto-compressed, 2 verbatim; **no translate call** — every bound post is `language_detected: en` on an `en` config, so `_translate_wanted` is False for all nine (the correct answer; `source_language` is recorded on each). One topic `skip:PROMO`, all nine roster posts `en/fit`. Two `panel_counter_stripped` events.
- **Estimate at $0** (`budget.estimate` over the same plan, scratch `estimate_lines.py`): `target` worst case adds **9 × `translate_call` = $0.0198** on top of the nine `copy_call` lines; `copy_retry_allowance` 36 / `copy_split_allowance` 18; expected **$3.83**, worst case **$15.59** (vs `source` mode $3.81 / $15.47 — zero change when the mode is off). The cap gates on `expected_usd`, so `--budget 15` admits all nine.

## Final run — `20260821_030722_4344` (`hypedigitaly-fresh`, 9 carousels, `--budget 15 --yes --verbose`)

**Result:** exit 1 (partial-success, delivered with losses) · 38 m 45 s · **$7.77** of the $15 cap (render $4.90 = 98 jobs incl. 18 extra cover candidates and 16 gauntlet re-renders; LLM $2.87 = analysis $1.30 (slide intel 9 + cover pick 9 + matcher), critic $1.55, copy $0.01) · 9 planned → **7 delivered, 2 gauntlet-blocked** (`Li_car_claude-opus-sonnet…_07` circuit-atlas: `forbidden_mark`/`missing_mark`/`invented_text`; `Tk_car_ai-watermarking…_06` contrast-verdict: `invented_text` s1 ×2 rounds + `pair_break`). Funnel: 699 posts → 206 → 9 topics → 7 kept (2 `skip:PROMO`) → 9 decks over 7 topics, 6 styles.

**Acceptance against plan §9 (what could be verified):**

| §9 item | Result |
|---|---|
| ≥ 5 distinct styles of 9, none > 3 of 9 | ✅ **6 distinct** — `neon-glass-dark` ×3, `circuit-atlas-dark` ×2, `big-number-editorial`, `build-log-mono`, `terminal-mockup-deck`, `contrast-verdict-deck`; no FR-355 line (correct: 3/9 is not > 1/2). `big-number-editorial` 1 of 9 after the narrowing (M's preview: 5 of 9) |
| concentration line agrees with reality | ✅ silent, and reality is 3/9 max |
| `meta.yaml` carries `counter`, `copy_language`, `source_language`, `cover_pick`, per-row `compressed` / `translated` | ✅ all nine (`counter` detected on 4: `positional` ×2 — one of them the j867 post, **7 bare `01…07` lines stripped at admission**, `chrome_counter_stripped: true` ×7 — `denominator` ×2; `copy_language: source`, `source_language: en` on all nine; `cover_pick` chosen 1/2/3 spread, `degraded: false` ×9, `covers/` ×3 each; `compressed` rows on 5 decks, `translated: false` everywhere) |
| every shipped string in the config language | ✅ trivially — all nine bound posts are `language_detected: en` under an `en` config; **`_call_translate` was never exercised live** (next session, item 2) |
| cover best-of-3 by eye | ✅ `_08`: chosen 2 — "avoids window-chrome dots of candidate 1 and keeps headline within the 13–32 % band unlike candidate 3's overflow" — the winner IS `slide_01`; every strip present in the gallery |
| no `style_consistency → counter_value` ping-pong in any `GAUNTLET_REPORT.yaml` | ✅ zero `counter_value` / `counter_placement` codes in nine reports — but see the gauntlet finding below (6 decks unjudged) |
| counter on every slide of a counted deck, none on an uncounted one; no invented badge | ⚠️ MIXED by eye: `_03` (counted, `circuit-atlas-dark`) shows the `07` chip top-right ✅; `_08` (counted, `neon-glass-dark`) shows NO counter on slides 1 and 3 ✗ (render miss; the critic that would have ordered the fix could not read the deck); `_05` (UNCOUNTED, `big-number-editorial`) renders a big teal **"2"** on slide 2 whose quoted line is "I didn't start with AI agents." ✗ — the invented position numeral M's registry rule forbids, unjudged for the same reason |
| zero placeholder bars / empty bottom bands / duplicated rows | ✅ on the slides read by eye (`_08` s1/s3, `_03` s7, `_05` s2): no bars, no bands, no duplicated rows |
| no large flat saturated field; teal drift avg < 15 | ✅ drift script: grounds `#000809` sd 1.4 · `#FAF9F7` sd 0.6 · `#000809` sd 1.8 · `#0B0B0B` sd 0.5 · `#03060B` sd 0.7 · `#010306` sd 2.0 · `#010205` sd 0.8 · `#000203` sd 0.9 · `#020203` sd 0.8 — every ground at a value extreme, sd ≤ 2.0, all **2048 px**; no mid-tone field exists to drift any more (baseline `4a0q`: mid-teal strip 30.3/69) |
| grid test — covers read as one brand | ✅ by eye on the three covers read: near-black grounds, one teal accent each, type inside the safe area |

**🔴 The finding that dominates this run — the gauntlet is blind on decks ≥ 8 frames at 2K.** `gauntlet_critic_unavailable` ×22: every critic call on the 8-, 9- and 10-slide decks failed at OpenRouter with **"Invalid request. Error => Request body could not be parsed as JSON"** (`llm_call` rows: `image_count: 10`, 0 tokens, $0). A 2K slide is ≈ 4.7 MB PNG; ten of them base64-encoded are ≈ 60 MB in one JSON body. The 5–7-frame decks (`_03`, `_06`, `_07`) and the 3-frame cover picks were judged normally; the 9-frame `_04` lost one critic of three. So "6 stopped" = six decks delivered **unjudged** (`gate DEGRADED — brief, craft, system could not be read`), and the two render defects above shipped because nothing could see them. L flagged one such deck (`01`) and M's closeout item 6 said "suspect the image payload size" — this run confirms it and sizes it: the threshold sits between 7 and 8 frames at 2K. **Every fix needs an operator decision, so none was taken:** (a) attach the critic's frames as Kie result URLs instead of base64 — amends **FR-40** ("base64 only, never a URL"; our own render URLs live ≈ 24 h and the critic runs minutes after render); (b) downscale frames for the CRITIC path only — **FR-128/FR-93** already say "downscaling is the caller's job" but CLAUDE.md's D48 rule limits Pillow to logo-patch cropping, so this is a second narrow carve-out; (c) judge in frame batches — changes the cross-frame consistency contract; (d) drop the critics to 1K renders — the render is 2K for colour, the critic could read a 1K re-fetch only if Kie served one (it does not). Recommendation: (a) for the critics and the cover pick alike (one payload path), with (b) as the fallback if OpenRouter rejects remote URLs at 2K.

**Smaller findings from the run:** `_03`'s source deck carries a sponsor header ("Jason AI by Reply" — a product, not the creator `@oleg.talk`) in every panel's words; FR-312 layer 3 drops creator identities, not sponsors, and the blocklist does not name it, so it rendered as a logo + wordmark on our slides — a follow-up for the filter's `brands_to_strip` guards or a sponsor-line heuristic. `_07` blocked on `forbidden_mark`/`missing_mark` (tool-logo fidelity, FR-315) after the per-deck gauntlet budget ($0.30) was spent. `Li_car_claude-ai-for-coding-development_04` passed after one fix round (`identity_leak` s7, `composition` s5).

## Code review (one read-only `code-reviewer` pass over the whole diff, during the paid run)

0 critical · 4 major · 7 minor · 3 nits; all fixed in-session except the two noted (fix round: +31 tests, suite 1879 → 1910; `plan.Assignment.off_language_posts` + `runner._off_language_line`, `slide_intel.bare_numeral_position`, `_source_language(entry, offer, run)` with rung 3, `copy_language` read off the rows, `drop_reason` carried through the splice, `only=None` under compress). Majors: (1) the `source`-mode bind-skip `logger.warning` in `plan.py` was swallowed by `__main__`'s `NullHandler` — replaced by DATA on the assignment result + one runner console line and `run.log` warning; (2) translate + compress/auto re-ran `_mapped_deck` on the translated offer and flattened every source `drop_reason` to `empty` (the gauntlet's `wordless_reason` would have said `empty_panel` for an FR-319 blank) — the row flags are now copied from the translated walk; (3) `copy_language: target` was written even when the already-target backstop shipped every row in source bytes — now `target` only when a row actually translated, and such a deck is tagged `copy_not_translated` (the ladder and the model disagreed; the operator should see it); (4) the bare-numeral admission strip acted on ONE slide while the detector needs two — admission now mirrors rule 2's corroboration. Design-level finding (5): with the LANG skip off under `target`, a topic the screen read as German whose posts carry no Virlo language and no vision reading shipped German pixels — the screen's own `Verdict.language` is now rung 3 of the ladder (`write_copy(topic_languages=…)`), so `target` can never be blinder than `source`. Minors: compress-mode follow-up sent the auto sibling line (`only=None` now), a stale `_language` docstring, three PRD sentences that over-promised (FR-344's 6,500-char target and a marker string that never existed; FR-346's `Copy: copy_not_translated` gallery line and a provenance list of drift lengths; FR-343's "stated in pre-flight") — PRDs amended to the shipped reality; a false cs-config comment about bound images/reels; the `_stage` column arithmetic (52, not 53); `en-US` printing two ways in the translate prompt. NOT changed: the bare `"source"` string literals in `plan`/`topic_filter` (importing `copywrite`'s constants would close an import cycle) — nit 14 stands as a note.

## Defects found that the plan did not contain

1. **PRD writer slips (second session in a row):** no D63 paragraph, no FR-344/FR-345 blocks, FR-294/100/313 unamended at their own lines, FR-343 claiming the panels are "humanised", an "unavailable analyst" failure door (the role is `copy`), a `mark_boxes` parenthetical on `translate_list_truncated`; after the correction round two wording slips remained (`(source mode)` on the target-mode example; `translated from de to en` where the gallery cannot know the target). Fixed; memory note `hypesocials-prd-writer-needs-audit-round` written.
2. **The gallery cannot name the target language** — `meta.yaml` records `source_language` but no target; the gallery reads documents alone. FR-346's example now says "to the platform's language". Adding `language` to `AssetRecord` would fix it properly (FR-73 change, not taken this session).
3. **`"German"` folded to `"ge"`** — `_LANGUAGE_ALIASES` knew only `en`/`cs` (D6) and the first-two-letters fallback is approximate. Widened with the common names; codes are what Virlo and the question template answer anyway.
4. **`plan.py` had a "no logging" invariant** — the bind-screen skip is the first thing `plan` reports on its own; amended in the module docstring with the reason (the drop reaches no result object; `runner` could not be touched by that agent). A later session may prefer an `Assignment` field + a runner print.
5. **`tests/test_copy_verbatim_filter.py` was in nobody's ownership row** and pinned an exact panel-map row dict; the `translated` key broke it (Agent A added the key, +6 lines).
6. **`measure_one_style.py` takes a draft FILE PATH, not a key** (the design said `<key>`); the prompt-engineer sliced one block into a temp draft. A `measure_key.py` helper exists in that session's scratchpad; promoting it to `plans/tools/` is a follow-up.
7. **Wave-1 twin staleness is visible to the suite** — the moment the prompt-engineer edited `slide_intel_question.md`, `test_template_parity` went red for the 9a and B agents; both reported it correctly as the conductor's splice. The splice tool closes that window.

## Deviations accepted (with reasons)

1. The translate template is **9,815 chars**, not ≤ 6,500: the fourteen humaniser bans (2,452 chars) + compress-identical fencing and hard bans are mandated verbatim; the remainder is the rules. One call per deck, under a cent per run.
2. `copy_mode` on a translated `compress`-mode deck is `compress`, not `auto` (every admitted row is in `over`, so `any(compressed)` would always say `auto`).
3. `translate_no_text` fires only when the MODEL answered nothing; an engine-rejected line already has its own warning.
4. The `source`-mode bind skip is logged from `_carousel_supply` (once per dropped post), not `fresh_source_post` (called per creative group — nine lines for one post).
5. `estimate()` gained keyword-only `post_languages` so the runner's post-ASSIGN re-price can drop already-English decks off the translate line while every other caller prices the D11 worst case.
6. `copy_retry_allowance` widened too (not only `copy_split_allowance`): FR-107's retry pair applies to every call and the translate prompt is the dearer one.
7. Both COPY-line shapes that fire together drop the `N call(s) ->` head (54 > 53 columns at `[7/10]`); the call count is on the stage's opening line.
8. The registry kept `photo-poster-statement`'s pre-existing handoff to two styles outside `ENABLED_SEVENTEEN` (not widened).

## Growth attribution (rule 5)

**Production +2,486** (37,128 → 39,614; the review-fix round is +287 of it — `copywrite.py` +144, `runner.py` +88, `slide_intel.py` +38, `plan.py` +17): `copywrite.py` +984 −29 (translate path, fifth sibling branch, `translated` rows, FR-313 admission, docstrings) · `prompts_engine.py` +257 (the translate twin +234, the slide-intel twin +5, `_ALLOWLIST` row +18) · `sources/slide_intel.py` +198 −13 (bare numerals, deck `language`) · `budget.py` +152 −9 · `runner.py` +133 −3 · `models.py` +102 (`CopyTranslated`, two tags, two record fields, registry rows) · `outputs/gallery.py` +89 −5 · `plan.py` +87 −7 · `topic_filter.py` +70 −15 · `preflight.py` +53 · `previews.py` +40 −10 · `menu.py` +35 · `cli.py` +25 · `generate/__init__.py` +20 −4 · `sources/virlo.py` +19 −1 · `virlo_mcp/server.py` +16 · `config.py` +15 (`wizard_help.md` +12 is not Python).
**Tests +2,610:** `test_copywrite.py` +758 · `test_slide_intel.py` +314 · `test_gallery.py` +314 · `test_console_inventory.py` +312 · `test_plan.py` +176 · `test_budget.py` +147 · `test_config.py` +136 · `test_virlo_data_channel.py` +132 · `test_preflight.py` +123 · `test_topic_filter.py` +92 · `test_menu.py` +82 · `test_prompts_engine.py` +26 · `test_topic_split.py` +16 · `test_template_parity.py` +13 −9 · `test_copy_verbatim_filter.py` +6.
**Non-code:** `prompts/copy_translate_system.md` new (234 lines) · `prompts/README.md` +50 −24 · `prompts/slide_intel_question.md` +5 · `prompts/styles.yaml` +35 −20 (four profiles + header note) · four configs (+35 default, +29 −8 cs, +5 ×2) · PRDs (`00-overview` +25 −13, `10-pipeline` +9 −6, `20-integrations` +2 −2, `30-configuration` +12 −8, `40-outputs` +9 −5, `50-promptcraft` +5 −3, `PRD.html` +12 −6) · `CLAUDE.md`/`AGENTS.md` +3 glossary entries + 2 amended (hardlink rebuilt, same inode, `cmp` identical) · `NAVIGATION.md` §3 ×3, §13 · `plans/EXECUTION-ORDER.md` J–N ✅ · `plans/SESSION-N-DESIGN.md` new · `plans/tools/splice_builtin_twin.py` new.
No docstring, comment or error message was shortened anywhere. **Splitting candidates (§3a, design call, not done):** `copywrite.py` 4,899 (the translate block ~900 lines would stand as `copywrite_translate.py` beside a future `copywrite_compress.py`), `prompts_engine.py` 4,359 (the `_BUILT_INS` table alone is ~2,700 lines — a `prompts_builtins.py` module is the obvious cut), `carousel.py` 2,171, `runner.py` 2,600+, `budget.py` 1,540, `slide_intel.py` 1,372.

## PRD conflicts

None outstanding. Two wording slips found and fixed in-session (Defect 1). Still owed from Sessions H/I (unchanged): the three D15 rulings in `SESSION-I-CLOSEOUT.md:87`.

## ⚠️ What the next session must do first

0. **Operator decision on the gauntlet payload at 2K** (the run's dominant finding, above): the critics cannot read a deck of ≥ 8 frames — choose (a) Kie result URLs for critic + cover-pick attachments (amend FR-40), (b) a critic-only downscale carve-out (FR-128/FR-93 vs the D48 Pillow rule), or (c) batching. Until then every 8+-slide deck ships unjudged and `gate DEGRADED` is the normal case, not the exception — which also means the two render misses seen by eye (`_08` missing counter, `_05` invented numeral) are what the gate would have caught.
1. **Merge J → N to `main`** — five commits on `session-k-colour-type-spine`, PR body in `plans/PR-BODY-sessions-j-n.md`.
2. **A real translate run has never happened.** Every bound post this week was English under an English config, so `_call_translate` has only been exercised by tests. First live exercise options: `--config hypedigitaly-cs --carousels 3 --budget 5` (English posts → Czech decks, the symmetric flip) — read one `meta.yaml` for `copy_language: target`, `source_language: en`, per-row `translated: true`, the gallery chip, and the `translated` provenance row; check `events.jsonl` for `translate_length_drift` / `translate_already_target`; then re-base `_TRANSLATE_*` token constants against the measured `llm_call` rows (D11: only ever move them against a measurement).
3. **`big-number-editorial` now matches 0 of 9** (was 5 of 9). Watch the next two previews: if it stays at 0 on weeks with genuinely numbered decks, the profile is too narrow and the FR-341 handoff table needs a sentence on "panels whose first token is a number OR a `step N` label".
4. `AssetRecord.language` (the platform's configured language) would let the gallery say "to en" — an FR-73 amendment, one field, one `_record` line.
5. Promote the scratchpad `measure_key.py` (slice one style block out of `styles.yaml` into a draft for `measure_one_style.py`) to `plans/tools/`.
6. Follow-ups carried from K/L/M: split `styles.py`'s palette section; delete `prompts_engine`'s five dead pre-F20 constants; `pytest-randomly` not installed; `PRD.html` artifact republish (v2.7.0 now); ASSIGN receipt cannot distinguish `-teal` variants; dropped-row gallery chip cosmetics; `meme-caricature-panels` dead 110-char headline cap; `previews._assign_block` logs no `style_concentration` event; `_COVER_PICK_ASSUMED` re-base once a paid run has real `cover_pick_call` token counts (this run has them — see `events.jsonl`); §3a deep-module reviews listed above.
7. The three preview run folders (`…_osj7`, `…_x0ug`, and M's `…_a440`/`…_0wfg`) are log-only and can be deleted; kept as evidence for the closeouts.
